# Pre-Commit Checklist

> Hook-enforced: any unchecked-box line blocks the commit. Mark `[x]` when done; for N/A items use `[x] N/A: <reason>`. Silent skip is forbidden. Post-commit hook auto-resets boxes to unchecked for the next commit.

- [x] **Branch + scope** — on `claude/practical-swanson-4b6468` (verified via `git branch --show-current` in worktree); atomic unit: new design spec `docs/superpowers/specs/2026-05-06-scenarios-recalibration-design.md` documenting the recalibrated 3 cliff-test scenario grids + N=5 pilot acceptance criteria with small-N statistical corrections per 2026-05-06 red-team operational addendum (1 file, +152 lines, 0 deletions).
- [x] **Verification** — `git diff --cached` scanned (will perform pre-commit); no secrets, no API keys, no PII. `/check-agent` N/A: doc-only commit, no Python touched.
- [x] **Review** — N/A: doc-only per REVIEW.md taxonomy. Spec content was the cognitive work and was user-approved across two red-team rounds (matrix approval + addendums approval) in this session. Per `feedback_subagent_dispatch_selectivity`, manual Layer-1 reviewer dispatch is reserved for the 4 cases; doc-only spec authoring is not one of them. Layer 1.5 pre-push hook will still run code-review at push time as backstop.
- [x] **Documentation** — this commit IS the documentation. The spec file documents context, pinned decisions, three revised matrices with structural justification, code-side deltas, pilot re-calibration target with addendum-corrected acceptance criteria, out-of-scope deferrals, and implementation handoff. References original `2026-05-05-cliff-test-scenarios-design.md` framework + ADR-0006 Amendment 1 Sonnet ToT path.
- [x] **Commit message** — `docs(specs): Phase 0.7 cliff-test scenarios recalibration` (57 chars, ≤72); body explains pilot calibration failures, scope boundary (framework unchanged, only 3 grids + acceptance criteria), and red-team addendum corrections; co-author tag present.
