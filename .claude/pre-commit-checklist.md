# Pre-Commit Checklist

> Hook-enforced: any unchecked-box line blocks the commit. Mark `[x]` when done; for N/A items use `[x] N/A: <reason>`. Silent skip is forbidden. Post-commit hook auto-resets boxes to unchecked for the next commit.

- [x] **Branch + scope** — on `claude/methodology-citation-rewrite` (sub-branch off `main`, mirroring the PR #10 pattern; long-lived branch `claude/practical-swanson-4b6468` already has PR #11 open with the external-review arc). Atomic unit: `docs/product/methodology.md` citation rewrite per `docs/external-review/round-3-synthesis.md` §7(b)#1, addressing C4 / H8 / H10. Drops Yehuda; reframes Russell as inspiration for 2 of 6 dimensions (valence, arousal); flags the other 4 (anxiety, frustration, confidence, dopamine) as Nova-engineered operational primitives; demotes Schultz to conceptual-lineage-only and adds Daw + Niv as operational anchor for agentic-timescale model-based RPE; reframes State-Transition Signatures §1.1–1.4 as engineered conjunction predicates (renames "Literature anchor" → "Inspiration"); reframes §1.6.1 affective-baseline row + memory-decay table column as engineered defaults; tightens §7 Ebbinghaus/Murre/Bahrick entries for consistency. 1 file, +101 / -37 lines, 0 code touched. §4.2 Levene's swap deferred to PR 6.
- [x] **Verification** — `git diff --cached` scanned (no secrets, no API keys, no PII; citation rewrite only). `/check-agent` N/A: doc-only, no Python touched. `/check-viewer` N/A: doc-only, no TS touched.
- [x] **Review** — N/A: doc-only per REVIEW.md taxonomy (`docs/**/*.md` row). Per `feedback_subagent_dispatch_selectivity` the 4 manual-Layer-1 cases don't apply to citation rewrites. Layer 1.5 pre-push hook + PR-time `claude-code-action` (Layer 2) provide backstop coverage.
- [x] **Documentation** — methodology.md IS the documentation. Section-by-section rewrite addresses C4 (citation appropriation), H8 (Russell-timescale mismatch), H10 (signatures framed as literature-anchored vs engineered).
- [x] **Commit message** — `docs(methodology): C4 citation rewrite — affect anchors + signatures` (67 chars, ≤72); body cites synthesis attacks + scope; co-author tag present.
