# Pre-Commit Checklist

> Hook-enforced: any unchecked-box line blocks the commit. Mark `[x]` when done; for N/A items use `[x] N/A: <reason>`. Silent skip is forbidden. Post-commit hook auto-resets boxes to unchecked for the next commit.

- [x] **Branch + scope** — on claude/practical-swanson-4b6468; atomic unit: implementation of ADR-0006 Amendment 1 — `tiers.production.tot` → `claude-sonnet-4-6`, plus the two regression-test assertions that pinned the old value, plus the `cliff_test._build_llms` comment block referencing Pro (now stale, updated to reference Sonnet path) (4 files, +24/-12 lines).
- [x] **Verification** — `git diff --cached` scanned, no secrets; `/check-agent` trio clean (267 pytest passed including the renamed `test_production_uses_sonnet_for_tot` and the updated `test_build_llms_passes_thinking_budget_for_gemini_models` assertion; mypy strict clean; ruff clean).
- [x] **Review** — N/A: paired with ADR-0006 Amendment 1 commit `4575294` from this session. The amendment IS the deliberation artifact; this implementation commit is the mechanical wiring. REVIEW.md taxonomy `nova-agent/src/**` would normally require code-reviewer — skipping in-session because (a) the change is one constant + the test assertions that pin it; (b) Layer 1.5 pre-push hook runs code-review at push time; (c) ADR amendment was the cognitive work and was reviewed at design time. Per `feedback_subagent_dispatch_selectivity`, manual Layer-1 dispatch reserved for the 4 cases — ADR-paired mechanical implementation is not one of them.
- [x] **Documentation** — `tiers.py` module docstring updated to note the production tier now uses Sonnet for ToT; inline comment cross-references ADR-0006 Amendment 1 + the 2026-05-06 pilot evidence; renamed test docstring explains the rationale; `cliff_test._build_llms` comment block updated to remove the stale Pro-rejects-0 claim and add forward-compatible note for re-routing back to Pro.
- [x] **Commit message** — `fix(tiers): production.tot → claude-sonnet-4-6 per ADR-0006 Am 1`, body explains the wiring scope (constant + 2 test pins + comment) and references the ADR commit, co-author tag present.
