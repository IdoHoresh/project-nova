# Pre-Commit Checklist

> Before EVERY commit, work through this list and check each item.
> The pre-commit framework runs `scripts/check-claude-checklist.sh`
> which **blocks the commit** if any item is left unchecked.
>
> After a successful commit, `scripts/reset-checklist.sh` runs at the
> post-commit stage and resets every box back to `[x]` so the next
> commit starts fresh.
>
> If a step doesn't apply, write a one-sentence reason after the
> bracket (e.g. mark `[x]` and add `/review — skipped, doc-only change`)
> and check the box. Silent skipping is forbidden.

## Branch + scope

- [x] On feature branch `claude/practical-swanson-4b6468`, not `main`
- [x] `git diff --cached --stat` reviewed — 2 modified files: `nova-viewer/pnpm-workspace.yaml` (replaces stale `ignoredBuiltDependencies` placeholder block with proper `allowBuilds: <pkg>: true` config that pnpm 11.0.4 actually reads) + `nova-viewer/pnpm-lock.yaml` (auto-regenerated by pnpm install picking up the new allowlist; ~221 byte delta)
- [x] Atomic commit — single logical change: pnpm 11 build-script allowlist for esbuild, sharp, unrs-resolver. Without this, `pnpm run dev` fails with `ERR_PNPM_IGNORED_BUILDS` on a clean install — the workspace.yaml shipped placeholder text (`set this to true or false`) instead of real config.

## Verification

- [x] `git diff --cached` scanned for secrets — no env values / API keys / tokens; pnpm config + lock-file regeneration only
- [x] `nova-agent/` not touched — N/A, viewer-only fix
- [x] `nova-viewer/` touched — yes; verified `pnpm install` succeeds (build scripts run cleanly: esbuild postinstall, sharp install, unrs-resolver postinstall) AND `pnpm run dev` starts the Next.js dev server on localhost:3000 (HTTP 200 verified earlier this session)
- [x] Docs / config — `pnpm-workspace.yaml` is the canonical pnpm 11 location for build-script allowlist (moved here from `package.json` in pnpm v10+). Three deps (esbuild, sharp, unrs-resolver) all need to compile native bits; allowing them is the project-wide expected state. Lock file regeneration is mechanical.

## Review

- [x] `/review` dispatched on staged diff — N/A: lockfile + pnpm config are both "skip with reason: mechanical" rows of the REVIEW.md path-matched trigger taxonomy
- [x] `code-reviewer` subagent — N/A, covered by skip reason above
- [x] `security-reviewer` — N/A, no secrets / env / LLM / bus paths touched. Note: enabling build scripts grants those 3 deps the ability to run arbitrary code at install time. esbuild + sharp + unrs-resolver are all major widely-audited packages with millions of weekly downloads; the threat model accepts this risk for the build-tooling tier.

## Documentation

- [x] LESSONS.md — N/A on this commit; the underlying gotcha ("pnpm 11 requires `allowBuilds: <pkg>: true` in `pnpm-workspace.yaml`, not the placeholder text the tool generates by default") could merit a CLAUDE.md gotcha entry but is captured in the next-session resume-point memory for now; promote to LESSONS only if a second contributor hits it.
- [x] CLAUDE.md "Common gotchas" — DEFERRED. Worth a future entry ("pnpm 11 build-script allowlist lives in workspace.yaml as `allowBuilds:`, NOT the placeholder `ignoredBuiltDependencies` section that pnpm scaffolds"). Not adding here to keep this commit atomic; can ride along on a future viewer-touching commit.
- [x] ARCHITECTURE.md — N/A, build-tooling-only change
- [x] New ADR — N/A, no architectural decision; tactical pnpm-config fix

## Commit message

- [x] Conventional Commits format: `chore(viewer): allow esbuild+sharp+unrs-resolver build scripts (pnpm 11)`
- [x] Body explains *why* — `pnpm-workspace.yaml` shipped a placeholder block (`ignoredBuiltDependencies` + literal `set this to true or false` text) instead of real config. pnpm 11.0.4 fails install with `ERR_PNPM_IGNORED_BUILDS` until the 3 native-compile deps (esbuild, sharp, unrs-resolver) are explicitly allowed via `allowBuilds:`. Replaced placeholder with real allowlist + regenerated lock file. Verified `pnpm install` clean + `pnpm run dev` starts dev server on localhost:3000.
- [x] Co-author tag present: `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>`
