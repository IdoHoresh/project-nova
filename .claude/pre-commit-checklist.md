# Pre-Commit Checklist

> Hook-enforced: any unchecked-box line blocks the commit. Mark `[x]` when done; for N/A items use `[x] N/A: <reason>`. Silent skip is forbidden. Post-commit hook auto-resets boxes to unchecked for the next commit.

- [x] **Branch + scope** — on `claude/practical-swanson-4b6468`; atomic unit: new implementation plan `docs/superpowers/plans/2026-05-06-scenarios-recalibration.md` for the spec at `docs/superpowers/specs/2026-05-06-scenarios-recalibration-design.md` (commit `13d14eb`). Plan covers Task 1 (TDD impl: scenarios.py + 3 regression tests + 1 stale-comment fix), Task 2 (N=5 pilot at production tier Sonnet ToT), Task 3 (4-criterion §5 acceptance adjudication). 1 file, +291 lines, 0 deletions.
- [x] **Verification** — `git diff --cached` will be scanned pre-commit; no secrets, no API keys, no PII (plan content is procedural). `/check-agent` N/A: doc-only commit, no Python touched.
- [x] **Review** — N/A: doc-only per REVIEW.md taxonomy. Plan content was the cognitive work just produced via `superpowers:writing-plans` skill in this session and self-reviewed (spec coverage / placeholder scan / type consistency). Per `feedback_subagent_dispatch_selectivity`, manual Layer-1 reviewer dispatch is reserved for the 4 cases; doc-only plan authoring is not one of them. Layer 1.5 pre-push hook will run code-review at push time as backstop.
- [x] **Documentation** — this commit IS the documentation. Plan references the spec by path + commit hash, explicitly enumerates files modified, includes full code blocks for every step, and ends with PASS/FAIL/BORDERLINE branching to the deferred `analyze_results.py` spec or to a second-round amendment.
- [x] **Commit message** — `docs(plans): scenarios recalibration impl plan + N=5 pilot adjudication` (66 chars, ≤72); body explains scope (1 atomic commit + pilot + acceptance check), references the spec commit, notes single-session execution profile, co-author tag present.
