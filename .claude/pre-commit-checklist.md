# Pre-Commit Checklist

> Hook-enforced: any unchecked-box line blocks the commit. Mark `[x]` when done; for N/A items use `[x] N/A: <reason>`. Silent skip is forbidden. Post-commit hook auto-resets boxes to unchecked for the next commit.

- [x] **Branch + scope** — on `claude/practical-swanson-4b6468`; atomic unit: revert pre-push hook model from Sonnet 4.6 → Haiku 4.5 in `.claude/settings.json`. User-approved option (ii) from token-efficiency plan (Option B') after a 30-min investigation of commit 245e7d3 (the original 2026-05-04 Haiku→Sonnet bump). Trade: ~$25/sprint quota relief; Layer 1.5 catches less architectural drift, relying on Layer 2 PR Opus + TDD as backstops. 1 file edit (settings.json: model alias, statusMessage, _hooks_explanation block); plus this checklist update.
- [x] **Verification** — gate trio N/A: claude-tooling-only, no Python (`/check-agent` N/A) or TypeScript (`/check-viewer` N/A) touched. JSON syntax preserved (3 small string-field edits, no structural changes). Re-activation: `/hooks` reload OR session restart needed for the settings watcher to pick up the new model field. Standard caveat for any settings.json change.
- [x] **Review** — N/A: Claude-tooling-only per REVIEW.md taxonomy. Layer 1.5 pre-push hook (now on Haiku 4.5 after this commit takes effect on next session) will run as backstop on subsequent pushes.
- [x] **Documentation** — `_hooks_explanation` block updated inline: documents current Haiku 4.5 + revert date + rationale (quota relief) + cost figures (~$0.02-0.05/push, ~$13/sprint) + cross-reference to original bump commit 245e7d3 for full history. LESSONS.md still says "Layer 1.5 — Haiku" which becomes correct again after this revert; the L1.5 = Sonnet entries elsewhere may need a sweep in a later cleanup batch (deferred per Option B').
- [x] **Commit message** — `chore(claude): revert pre-push hook model to Haiku 4.5` (50 chars, ≤72); body explains the revert reason (quota pressure), references commit 245e7d3 for prior reasoning, notes user's explicit approval, lists all three settings.json fields touched, and includes the re-activation caveat. Co-author tag present.
