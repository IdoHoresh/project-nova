# Pre-Commit Checklist

> Before EVERY commit, work through this list and check each item.
> The pre-commit framework runs `scripts/check-claude-checklist.sh`
> which **blocks the commit** if any item is left unchecked.
>
> After a successful commit, `scripts/reset-checklist.sh` runs at the
> post-commit stage and resets every box back to `[x]` so the next
> commit starts fresh.
>
> If a step doesn't apply, write a one-sentence reason after the
> bracket (e.g. mark `[x]` and add `/review — skipped, doc-only change`)
> and check the box. Silent skipping is forbidden.

## Branch + scope

- [x] On feature branch `claude/practical-swanson-4b6468`, not `main`
- [x] `git diff --cached --stat` reviewed — 1 new file: `docs/superpowers/plans/2026-05-04-game2048sim.md` (~1300 lines, executable implementation plan generated by superpowers:writing-plans from spec 1001be5 + ADR-0008 b743eef)
- [x] Atomic commit — single logical change: writing-plans output for Game2048Sim. Implementation commits land in subsequent task-by-task commits per the plan.

## Verification

- [x] `git diff --cached` scanned for secrets — no env values / API keys / tokens; pure plan markdown with code blocks demonstrating tests + impl + commit shapes
- [x] `nova-agent/` not touched — N/A, doc-only plan; implementation lands per task in subsequent commits
- [x] `nova-viewer/` not touched — N/A
- [x] Docs / config — `docs/superpowers/plans/2026-05-04-game2048sim.md` follows the convention of the prior plan at `docs/superpowers/plans/2026-05-03-agent-event-validator.md` (header with required sub-skill pointer + goal/architecture/tech-stack, File Structure table, numbered tasks with bite-sized 2-5 minute steps, no placeholders per the writing-plans skill rule)

## Review

- [x] `/review` dispatched on staged diff — N/A: `docs/**` is the "skip with reason: doc-only" row of the REVIEW.md path-matched trigger taxonomy
- [x] `code-reviewer` subagent — N/A, covered by skip reason above. The plan describes future code; that code's review happens task-by-task as it lands.
- [x] `security-reviewer` — N/A, no secrets / env / LLM / bus paths touched (paths are described in the plan but no implementation is committed here)

## Documentation

- [x] LESSONS.md — N/A on this commit; conditional Task 7 in the plan handles any non-obvious surprises that surface during execution
- [x] CLAUDE.md "Common gotchas" — N/A
- [x] ARCHITECTURE.md — N/A on this commit; Task 5 of the plan includes the ARCHITECTURE.md update for the GameIO seam
- [x] New ADR — N/A on this commit; ADR-0008 (b743eef) already documents the architectural commitments the plan implements

## Commit message

- [x] Conventional Commits format: `docs(plans): add Game2048Sim implementation plan`
- [x] Body explains *why* — writing-plans output from spec 1001be5 + ADR-0008 b743eef. 7 tasks (5 feat + 1 chore calibration smoke + 1 conditional docs lessons), ~32 new tests, all TDD-first per nova-agent rules. Plan is executable by a fresh Claude session in a new context — primary anchor for the Game2048Sim implementation chain.
- [x] Co-author tag present: `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>`
