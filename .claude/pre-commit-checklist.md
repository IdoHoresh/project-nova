# Pre-Commit Checklist

> Hook-enforced: any unchecked-box line blocks the commit. Mark `[x]` when done; for N/A items use `[x] N/A: <reason>`. Silent skip is forbidden. Post-commit hook auto-resets boxes to unchecked for the next commit.

- [x] **Branch + scope** — on `claude/practical-swanson-4b6468`; atomic unit: new file `docs/external-review/2026-05-06-comprehensive-review-prompt.md` (~400 lines). Adversarial red-team review mandate for an external reviewer (Claude + Gemini in parallel per user's stated workflow). Supersedes the stale `docs/product/external-review-brief.md` (kept as historical pre-pivot record). 1 file, +401 lines, 0 deletions, no code touched.
- [x] **Verification** — `git diff --cached` will be scanned pre-commit; no secrets, no API keys, no PII (review prompt is procedural). `/check-agent` N/A: doc-only commit, no Python touched. `/check-viewer` N/A: doc-only commit, no TS touched.
- [x] **Review** — N/A: doc-only per REVIEW.md taxonomy (`docs/**/*.md` row). Content is a review-request artifact, self-authored in this session via two iteration rounds with the user (first draft → adversarial-frame rewrite per user feedback). Per `feedback_subagent_dispatch_selectivity`, manual Layer-1 dispatch reserved for the 4 cases; doc-only review-prompt authoring is not one of them. Layer 1.5 pre-push hook will run code-review at push time as backstop.
- [x] **Documentation** — this commit IS the documentation. The new file is self-contained: explicit mandate (§1), repo pointer (§2), reading order (§3), ground-truth project state (§4), code paths under scrutiny (§5), 10 attack dimensions (§6), concession-test list (§7), strict response format (§8), logistics (§9). Cross-references the existing stale brief in §intro to mark supersession.
- [x] **Commit message** — `docs(review): adversarial red-team external-review mandate` (58 chars, ≤72); body explains scope (review prompt for Claude+Gemini parallel review), references the superseded `external-review-brief.md`, co-author tag present.
