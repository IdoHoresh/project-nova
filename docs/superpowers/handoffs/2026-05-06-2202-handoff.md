---
created: 2026-05-06T22:02:01Z
branch: claude/methodology-trauma-rewrite
sha: 4069ebb6198867d250c66a422afe541fe529df48
---

## Context Checkpoint — 2026-05-06 22:02 UTC

**Last shipped:** PR #18 merged — workflow-simplification batch (12 commits: `f44b20f` pre-flight → `4069ebb` Task 9). Cuts per-turn context load + token cost via tier-1-only `workflow.md` rewrite (234 → 91), `/commit-push-pr` + `/handoff` slash commands, Layer 2 PR action path-split (Sonnet routine + Opus deep), Haiku as default subagent model, Sonnet 200k as default main session, removal of pre-commit-checklist ceremony + Layer 1.5 Haiku push-gate.

**Architectural intent:**
The batch is a meta-workflow simplification, not a feature. Driving constraint: rule density was forcing partial reads → silent skips → guilt loop. Fix is tier separation (Tier-1 invariants survive in `workflow.md`; Tier-2 advisories live in `REVIEW.md` / slash commands / hook config — single source of truth per concern). Hooks own deterministic enforcement; LLMs own judgment-at-coherent-diff (Layer 2 only). The model rule moved from a one-line summary to a 9-row binary trigger table in CLAUDE.md so Sonnet can match against path globs and keywords instead of vibes-judging "is this Opus-territory?". Concurrent independent changes (Steps 6 + 7) use atomic commits + an ordered-rollback procedure in spec Section 10 — revert higher-variance change first if Section 9 trigger fires (2+ Layer 2 BLOCKs OR any single model-quality-diagnosed BLOCK on next 5 PRs).

**Edge-case warnings:**
- Pre-push deterministic hook's regex false-positives on `ANTHROPIC_API_KEY` literal-string mentions in YAML / spec / plan docs (regex matches the field name, not an actual key). Known, expected. Follow-up: scope regex to non-`.md` files OR require `=` followed by non-empty value.
- `anthropics/claude-code-action@v1` rejects OIDC token exchange on the first PR that modifies the workflow file itself ("Workflow validation failed" — workflow on PR branch differs from `main`). Documented behavior; PR #18 hit this on routine-review. Resolves automatically once new workflow lands on `main`. Future PRs will pass.
- Reviewer subagents hallucinated numerical claims twice during the batch — Task 2 reviewer claimed `permissions.deny` was empty (actually 39 entries, unchanged); Task 9 implementer claimed CLAUDE.md was 119 lines (actually 219, only ~7-line drop). Both caught by direct `git`/`jq` checks. Future practice: verify subagent numerical claims directly, do not trust report alone.
- `.claude/settings.local.json` and `~/.claude/projects/-Users-idohoresh-Desktop-a/memory/*.md` are NOT in git. Task 7's rollback is a manual file edit, not `git revert`. Plan Task 7 documents the manual revert command; spec Section 10 acknowledges the deviation.
- Claude Code memory paths are path-mangled per project location. `/handoff` works around this with a dual-write (portable git-tracked artifact in `docs/superpowers/handoffs/` + memory file). LESSONS.md captures the fragility.

**Rejected alternatives:**
- Defer Step 6 (Haiku subagent default) to a follow-up batch (red-team Option A) — rejected: token-savings urgency + atomic commits + ordered-rollback procedure recreate the diagnostic-attribution capability at near-zero cost.
- Keep Layer 1.5 Haiku push-gate — rejected: LLM-per-push burns tokens for marginal lift over deterministic gitleaks; Layer 2 catches architectural drift better at coherent-diff scope.
- `.claude/settings.local.json` for Haiku default (per original spec text) — rejected: gitignored, not git-revertable, breaks Section 10 ordered-rollback procedure. Moved to committed `.claude/settings.json` env field.
- `1.5× pre-batch baseline` BLOCK-rate rollback trigger — rejected: zero-baseline pathology (1.5 × 0 = 0). Replaced with "2+ BLOCKs on next 5 PRs OR any single BLOCK diagnosed as model-quality." Categorical signal beats multiplicative threshold on small windows.
- Exclude `.github/workflows/**` from Layer 2 routine-review — rejected: CI config is exactly the surface where silent misconfiguration breaks the safety net. Self-review is correct per second red-team round.
- Commit-and-deploy with `--no-verify` to bypass hook ordering issues during Task 1 — rejected: violates project rules. Split Task 1 into 1a (remove hook config) + 1b (delete artifacts) for clean atomic commits.

**What's next (priority-ordered):**

1. **Optional follow-up: residual CLAUDE.md trim** — Task 9 only dropped 7 lines (226 → 219). Plan target was ≤140, ideal ≤120. Trim candidates: collapse "Build + test" into `/check-agent` + `/check-viewer` references; delete "When in doubt" section. ~30 min, single PR, Sonnet 200k handles solo. Captures the full ~30% per-turn auto-load reduction the batch aimed for.

2. **Substantive: resume cognitive-architecture work per old resume point** — N=1 smoke test on snake-collapse-128 (`NOVA_TIER=production NOVA_SESSION_CAP_USD=3.0 uv run cliff-test --scenario snake-collapse-128 --n 1 --pilot`). Needs emulator + API keys + user authorization. Per the prior 2026-05-06 resume entry above this checkpoint, the scenario-recal + M-07 gate code is committed on `claude/scenarios-recalibration-code` (3 commits ahead of main, not yet PR'd) — verify branch state before resuming.

3. **Tag follow-ups in LESSONS.md** when convenient: refine pre-push regex to skip `.md`/`.yaml` docs; reviewer-numerical-claim verification habit (already mentioned in the 2026-05-07 LESSONS entry but worth promoting if it recurs).

**Read first on resume:**
- `CLAUDE.md` (219 lines; model trigger table at line 138; trimmed sections may still need follow-up)
- `.claude/rules/workflow.md` (91 lines; tier-1 invariants only; references CLAUDE.md + REVIEW.md + hooks for everything else)
- `docs/superpowers/specs/2026-05-07-workflow-simplification-design.md` (Section 10 ordered-rollback procedure if Layer 2 BLOCK-rate spikes)
- `docs/superpowers/plans/2026-05-07-workflow-simplification.md` (Task 7 manual rollback commands)
- `LESSONS.md` "Workflow / process learnings" — new "Concurrent independent workflow changes need explicit per-change rollback ordering" entry; "Engineering / debugging gotchas" — new migrated `[migrated from CLAUDE.md] nova-viewer requires pnpm, not npm` entry
- `git log --oneline -15` for recent commit context
- This handoff: `docs/superpowers/handoffs/2026-05-06-2202-handoff.md`
