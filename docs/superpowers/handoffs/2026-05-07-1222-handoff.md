---
created: 2026-05-07T12:22:30Z
branch: claude/n5-pilots-corner-512
sha: 9b94e39
---

## Context Checkpoint — 2026-05-07 12:22

**Last shipped:** `9b94e39` — fix(lab): recalibrate corner-abandonment-256 window (18,44) + adjudication

**Architectural intent:**
corner-abandonment-256 is fully adjudicated (N=6 PASS, C2 yellow-flag caveat) and committed. The window shift (12,17)→(18,44) follows the same calibration-criterion-not-product-criterion rationale used for snake in PR #22; an Option B fresh N=1 trial was added first because the +27 upper shift was wider than snake's +4 precedent. The 512-wall scenario has failed 5 successive grid iterations and now requires a structural redesign — Opus proposed a completely different layout (v6) that moves 512 from (3,3) to (3,2), puts 128 at (3,1), and leaves (3,3) empty to defer the structural doom signal from move 0 to moves 4–8.

**Edge-case warnings:**
- `scenarios.py` + tests currently have the **v5 grid** (uncommitted, gate-trio green): bottom row `[256, 32, 128, 512]`, `initial_score=7832`. This is a dead-end grid (infant mortality t_fail=3); must be overwritten with v6 before running any more trials.
- **nova-viewer drift**: `pnpm-lock.yaml`, `pnpm-workspace.yaml`, and `patches/` have uncommitted changes unrelated to lab work. Do NOT include these in the next lab commit.
- **M-07 is already fully implemented**: `BudgetedLLM` + `SessionBudget` in `budget.py` + `protocol.py`, wired into `cliff_test.py` lines 848–873. No M-07 work needed.
- **Opus v6 score correction**: Opus's handoff computed `initial_score=7352` but missed `(2,0)=8` (contributes 16). Correct `initial_score=7368`.
- **Fast-react root cause**: the (3,1) position in `[256, ?, 128, 512]` creates an obviously locked bottom row at t=0 regardless of center value ≤ 32. (3,1)=16 → fast-react. (3,1)=32 → infant mortality. (3,1)=64 → caps. All parameter variations exhausted — needed structural redesign.
- The 512-wall window `(12,17)` in `scenarios.py` will almost certainly need updating after v6 pilots (v3 actual distribution was [17,22,23,29,38]).

**Rejected alternatives:**
- v3: `(3,1)=16, (1,3)=0` — 0 caps but fast-react 2/5, median t_predicts=2
- v4: `(3,1)=16, (1,3)=32` — smoke halt, t_predicts=1 (Opus's partial suggestion)
- v5: `(3,1)=32, (1,3)=0` — infant mortality t_fail=3 (smoke halt)
- Spec §3.3 `(3,1)=32, (1,3)=32` — infant mortality 4/5 (known from earlier session)
- Tuning (3,1) endlessly — exhausted; structural redesign required

**What's next:**
Implement 512-wall v6 (Opus structural redesign): overwrite v5 grid in `scenarios.py` with `[[0,4,0,0],[4,8,16,32],[8,16,32,64],[256,128,512,0]]`, `initial_score=7368`. Update test pins in `test_lab_scenarios.py` and `test_lab_sim.py`. Run gate trio, then N=1 smoke. If smoke passes (t_predicts ≥ 2, bot > 8), run N=5 pilot. If C1 window mismatch, update window post-hoc (same logic as snake/corner). Then write adjudication doc + commit + open PR covering both corner and 512-wall.

**Read first on resume:**
- `CLAUDE.md`
- `docs/superpowers/specs/2026-05-06-scenarios-recalibration-design.md` (lab spec, still active)
- `git log --oneline -10`
- `~/.claude/projects/-Users-idohoresh-Desktop-a/memory/project_nova_resume_point.md`
