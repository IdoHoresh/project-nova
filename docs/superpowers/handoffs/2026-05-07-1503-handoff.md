---
created: 2026-05-07T15:03Z
branch: claude/phase-08-spec
sha: 832df96
---

## Context Checkpoint — 2026-05-07 15:03

**Last shipped:** `832df96` — docs(specs): Phase 0.8 trauma ablation design spec + LESSONS entry

**Architectural intent:**
Phase 0.8 falsifies methodology §4.2 dual-DV via a new standalone runner (not cliff_test.py extension).
The core IV is `trauma_enabled: bool` — a single config flag that suppresses `tag_aversive`, collapsing
the entire downstream chain (retrieval boost, extinction-over-time, inert cap) naturally. K=2 sessions
are load-bearing: game-1 is fresh-start with `force_trauma_on_game_over=True` (bypasses
`is_catastrophic_loss` predicate, which structurally cannot fire on fresh-start grids), game-2 is
measured. Without the bypass + 200-move cap, `tag_aversive` may never be written at all (50-move
production cap exits via range exhaustion, never reaching `_run_post_game`).

**Edge-case warnings:**
- `is_catastrophic_loss` conjunctive gate: `final_score < max_tile × 4` cannot be satisfied on any
  Phase 0.7 starting board because initial_score already exceeds threshold at t=0 (snake=1396 > 512,
  corner=3868 > 1024, 512-wall=7368 > 2048). Fresh-start + force_trauma bypass is the only correct
  game-1 shape. Do NOT use Phase 0.7 anchor grids as game-1.
- 50-move cap loophole: `main.py:204` `for step in range(50)` exits via range exhaustion if no game-over
  within cap; `_run_post_game` never fires; `tag_aversive` never written. MAX_MOVES_PHASE_08 = 200
  is mandatory, not optional.
- Pilot calibrates against conditional post-encounter trap-recurrence rate, NOT unconditional per-move
  base rate. Pilot must be K=2 sessions (not K=1) to replicate the main DV.
- D₄ orbit (24 anchor grids = 3 scenarios × 8 orientations) is mathematically free but the runner must
  apply the full orbit to each anchor before drawing one; otherwise analysis is not anchored-start.
- Stacked PRs: #27 base = `claude/phase-08-prep-doc-drift` (PR #26). PR #27 must not be merged until
  #26 merges to main first.

**Rejected alternatives:**
- Extend cliff_test.py for Phase 0.8 — cliff_test.py:434 hardcodes `trauma_triggered=False` (opposite
  of Phase 0.8 IV); 5-axis divergence from main Phase 0.8 structure would silently neuter the test.
- Use Phase 0.7 cliff scenarios as game-1 — arithmetic shows predicate never fires; would manufacture
  spec gap.
- One-tailed α=0.05 two-tailed N=87 — methodology §4.2 pre-registers direction; two-tailed is a
  methodology mismatch; N=70 is correct.
- Auto-abort after surrogate if d < d_floor — P(true d≥0.3 | obs=0.15) ≈ 25% at N=20; would kill
  ~25% of real-effect studies.

**What's next:**
1. Wait for PR #26 (`claude/phase-08-prep-doc-drift`) to merge to main.
2. Once merged, PR #27 auto-rebases; confirm it's still clean.
3. Fresh session: swap to Opus (`/model claude-opus-4-7[1m]`), invoke `superpowers:writing-plans`
   against `docs/superpowers/specs/2026-05-07-phase-0.8-trauma-ablation-design.md`.
4. writing-plans → subagent-driven-development for implementation.

**Read first on resume:**
- `CLAUDE.md`
- `docs/superpowers/specs/2026-05-07-phase-0.8-trauma-ablation-design.md`
- `docs/superpowers/handoffs/2026-05-07-1503-handoff.md`
- `git log --oneline -10`
