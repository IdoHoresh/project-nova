# Week 1 Security Review — 2026-05-01

## Checks performed
- [x] gitleaks detect --redact — clean (19 commits, 1.03 MB scanned)
- [x] GitHub secret_scanning + push_protection + dependabot — enabled
- [x] No env/secret/credential files in `git ls-files` (only `.env.example` patterns matched, excluded as expected)
- [x] `.env.example` has no populated values (every key is `KEY=` empty)
- [x] Pre-commit hook installed locally at `/Users/idohoresh/Desktop/a/.git/hooks/pre-commit` and verified executable
- [x] `.env` permissions set to `600` on both worktree and main repo copies

## GitHub security flags (snapshot)

```json
{
  "secret_scanning": "enabled",
  "secret_scanning_push_protection": "enabled",
  "dependabot_security_updates": "enabled",
  "secret_scanning_non_provider_patterns": "disabled",
  "secret_scanning_validity_checks": "disabled"
}
```

The two `disabled` flags require the paid **GitHub Secret Protection** SKU; on the free public-repo tier they cannot be toggled (API silently rejects PATCH attempts). Coverage substitute: the local `gitleaks protect --staged` pre-commit hook scans every staged change for non-provider patterns before the commit lands. Defense-in-depth is preserved.

## Findings
None.

## Action items
None blocking. Two latent items tracked separately:

1. `nova_agent.config.Settings()` reads `env_file=".env"` relative to cwd; when `nova` runs from `nova-agent/`, the user's repo-root `.env` won't be found. Pure-runtime concern; surfaces when the real loop boots (Task 9+).
2. `nova_agent.llm.cache` is implemented but not yet wired into adapter `complete()` calls. Implicit work the plan defers.

## Reviewer
Self-review (Ido).
