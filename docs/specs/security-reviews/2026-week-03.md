# Week 3 Security Review — 2026-05-01

## Scope
Closes Week 3 (Tasks 20–29): affect vector + RPE + decision-loop wiring; viewer adds MoodGauge, DopamineBar, AffectLabel, GameStream, BrainPanel, StatsFooter; pytest pythonpath fix.

## Checks performed
- [x] `gitleaks detect --redact` — clean (42 commits, ~1.11 MB scanned)
- [x] GitHub `secret_scanning`, `secret_scanning_push_protection`, `dependabot_security_updates` — all enabled
- [x] Dependabot alerts (open) — 0
- [x] No env/secret/credential files in `git ls-files` other than `.env.example` (root + nova-viewer); no real values in either
- [x] `.env` permissions still `600` on both worktree and main-repo copies
- [x] Pre-commit gitleaks hook covered all 10 commits this week (visible in commit output)

## GitHub security flags (snapshot)

```json
{
  "dependabot_security_updates": "enabled",
  "secret_scanning": "enabled",
  "secret_scanning_non_provider_patterns": "disabled",
  "secret_scanning_push_protection": "enabled",
  "secret_scanning_validity_checks": "disabled"
}
```

The two `disabled` flags require the paid GitHub Secret Protection SKU and remain unavailable on the public-repo free tier; defense-in-depth via the local pre-commit gitleaks hook is unchanged from Week 1.

## Dependency sanity (nova-agent)

```
anthropic        0.97.0
cryptography    47.0.0
fastapi          0.136.1
google-genai     1.74.0
lancedb          0.30.2
pydantic         2.13.3
pydantic-settings 2.14.0
tenacity         9.1.4
websockets      16.0
```

No CVE-flagged versions; Dependabot has no open advisories.

## Findings
None.

## Action items
None blocking. Latent issues tracked in `project_nova_resume_point.md`:

1. macOS Sequoia App Management auto-applies `UF_HIDDEN` to files under `~/Desktop`, and Python 3.14's site loader skips hidden `.pth` files. Mitigation in repo: `pythonpath = src` in `nova-agent/pytest.ini`. `uv run nova` and direct `import nova_agent` are still broken until the venv (or repo) moves off Desktop. Address before the next live emulator run (early Week 4 once trauma + ToT need the loop).
2. `nova_agent.llm.cache` still not wired into adapter `complete()` calls (carried from Week 1 latent list).
3. Gemini 2.5 Flash thinking-token wiring — `ReactDecider` uses `max_tokens=2000` to leave room for thinking + JSON; cleaner fix is per-model `thinking_budget=0` on `GeminiLLM`.
4. LanceDB `table_names()` deprecation warnings (replace with `list_tables()`).
5. `MemoryCoordinator.write_move` still passes `importance=1` placeholder; planned replacement in Task 25 affect-importance scoring (deferred to Week 4 once trauma tagging lands first).

## Reviewer
Self-review (Ido).
