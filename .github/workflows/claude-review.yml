# Claude PR Review (Layer 2) — DISABLED
#
# This workflow has been removed. In-session Opus review via /commit-push-pr
# replaces it. The /commit-push-pr skill auto-dispatches code-reviewer and
# security-reviewer subagents (model: opus) against every non-doc diff before
# commit — billed against Max subscription quota, not API key.
#
# Removed: 2026-05-07. Reason: CI review always billed ANTHROPIC_API_KEY
# regardless of plan. In-session review is higher quality (Opus vs Sonnet)
# and zero API cost.
#
# To re-enable: restore the `on:` trigger and jobs from git history.
